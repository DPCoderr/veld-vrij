# Aanvullingen op de gebruikte prompts

Onderstaande tekst is bij de genoemde generaties achter de opgeslagen schermprompt toegevoegd.

## 07-mijn-reserveringen-mobile

```text
MOBILE OVERRIDE: the reference is desktop styling only. Reflow to a TRUE 390px viewport with readable 16px body. Header contains only VeldVrij Demo and Menu; do not copy desktop links. Do NOT fit all content: crop naturally at the bottom. Shorten the title into 2 lines if needed. Prioritize the key action and only 1–2 cards above fold.
```

## 08-reserveringsdetail-mobile

```text
MOBILE OVERRIDE: the reference is desktop styling only. Reflow to a TRUE 390px viewport with readable 16px body. Header contains only VeldVrij Demo and Menu; do not copy desktop links. Do NOT fit all content: crop naturally at the bottom. Shorten the title into 2 lines if needed. Prioritize the key action and only 1–2 cards above fold.
```

## 09-zo-werkt-het-mobile

```text
MOBILE OVERRIDE: the reference is desktop styling only. Reflow to a TRUE 390px viewport with readable 16px body. Header contains only VeldVrij Demo and Menu; do not copy desktop links. Do NOT fit all content: crop naturally at the bottom. Shorten the title into 2 lines if needed. Prioritize the key action and only 1–2 cards above fold.
```

## 10-beheer-overzicht-mobile

```text
MOBILE OVERRIDE: reference is styling only. TRUE 390px viewport, readable 16px body, header only VeldVrij Demo and Menu. NO desktop sidebar or desktop navigation links. Reflow into one column; show only what naturally fits, crop below fold, never shrink all page content. Main controls min44px design height.
```

## 11-veldbeheer-mobile

```text
MOBILE OVERRIDE: reference is styling only. TRUE 390px viewport, readable 16px body, header only VeldVrij Demo and Menu. NO desktop sidebar or desktop navigation links. Reflow into one column; show only what naturally fits, crop below fold, never shrink all page content. Main controls min44px design height.
```

## 12-planning-mobile

```text
MOBILE OVERRIDE: reference is styling only. TRUE 390px viewport, readable 16px body, header only VeldVrij Demo and Menu. NO desktop sidebar or desktop navigation links. Reflow into one column; show only what naturally fits, crop below fold, never shrink all page content. Main controls min44px design height.
```

## 13-poortdemo-mobile

```text
MOBILE OVERRIDE: reference is styling only. TRUE 390px viewport, readable 16px body, header only VeldVrij Demo and Menu. NO desktop sidebar or desktop navigation links. Reflow into one column; show only what naturally fits, crop below fold, never shrink all page content. Main controls min44px design height.
```

## 14-velden-mobile

```text
MOBILE OVERRIDE: reference is styling only. TRUE 390px viewport, readable 16px body, header only VeldVrij Demo and Menu. NO desktop sidebar or desktop navigation. Replace the table entirely with stacked cards; never fit a wide table into mobile. Each card has name, labelled facts and action. Show only 1–2 fully readable cards then natural crop.
```

## 15-locaties-mobile

```text
MOBILE OVERRIDE: reference is styling only. TRUE 390px viewport, readable 16px body, header only VeldVrij Demo and Menu. NO desktop sidebar or desktop navigation. Replace the table entirely with stacked cards; never fit a wide table into mobile. Each card has name, labelled facts and action. Show only 1–2 fully readable cards then natural crop.
```

## 16-beheer-reserveringen-mobile

```text
MOBILE OVERRIDE: reference is styling only. TRUE 390px viewport, readable 16px body, header only VeldVrij Demo and Menu. NO desktop sidebar or desktop navigation. Replace the table entirely with stacked cards; never fit a wide table into mobile. Each card has name, labelled facts and action. Show only 1–2 fully readable cards then natural crop.
```

